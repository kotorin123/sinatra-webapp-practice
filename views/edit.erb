<h2>編集</h2>

<form action="/memos/<%= @memo_id %>" method="POST">
  <input type="hidden" name="_method" value="patch">

  <div class="form-area">
    <label>タイトル</label>
    <input type="text" value="<%= h @memos_list[@memo_id]["title"] %>" name="title" required>
  </div>

  <div class="form-area">
    <label>内容</label>
    <textarea name="content" required><%= h @memos_list[@memo_id]["content"] %></textarea>
  </div>

  <div class="actions">
    <input type="submit" value="変更">
    <a href="/memos">キャンセル</a>
  </div>

</form>
